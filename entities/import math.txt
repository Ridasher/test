import math

def generate_phyllotaxis(n):
    points = []
    golden_angle = 137.5

    for i in range(n):
        r = math.sqrt(i) * 5
        theta = math.radians(i * golden_angle)

        x = r * math.cos(theta)
        y = r * math.sin(theta)

        points.append((x, y))

    return points