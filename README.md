# LinkedIn IA - Assistant Personnel

Application fullstack (Vite + Express + Ollama) pour la génération de posts et messages LinkedIn.

## Structure

- `client/` : Frontend React + Vite
- `server/` : Backend Express + JSON DB

## Installation

```bash
npm run install:all
```

## Démarrage

```bash
npm run dev
```
